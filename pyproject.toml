[project]
name = "time-tracker"
version = "0.1.0"
description = "An app that tracks time spent using desktop applications like, browsers, vscode, etc"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui>=3.11.1",
    "pandas>=3.0.3",
    "pydantic>=2.13.4",
    "pywebview>=6.2.1",
    "pywin32>=311",
    "uuid>=1.30",
]
