[project]
name = "time-tracker"
version = "0.1.0"
description = "An app that tracks time spent using a desktop application like, browsers, vscode, and etc"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
    "pywin32>=311",
]
